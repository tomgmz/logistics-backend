# Node dependencies
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
.next/
dist/
out/

# Environment variables
.env
.env.local
.env.*.local
# Backup copies of .env. One of these (.env.bak-frontend-url) was swept into a
# commit by `git add -A` and caught by GitHub push protection; the old patterns
# matched .env and .env.*.local but not .env.bak-*.
.env.*
!.env.example
*.bak

# Production row snapshots taken before a destructive script runs
backups/

# OS files
.DS_Store
Thumbs.db

# IDE settings
.vscode/
.idea/
